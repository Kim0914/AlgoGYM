#include <iostream>
using namespace std;
#define OPTIMIZE ios_base::sync_with_stdio(false); cin.tie(0); cout.tie(0);

int tc = 0, a = 0, b = 0;
int main() {
	OPTIMIZE;
	cin >> tc;

	while (tc--) {
		cin >> a >> b;
		cout << "yes" << '\n';
	}

	return 0;
}
